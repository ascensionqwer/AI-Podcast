# Core dependencies
openai>=1.0.0
pyyaml>=6.0

# MLX-Audio for Kokoro TTS (Apple Silicon) - used when tts.provider="kokoro"
mlx-audio>=0.1.0
kokoro-onnx>=0.5.0

# Qwen3-TTS for Voice Cloning - used when tts.provider="voice_clone"
qwen-tts>=0.1.0
torch>=2.0.0
soundfile>=0.12.0

# TTS Server
fastapi>=0.100.0
uvicorn>=0.23.0
httpx>=0.25.0
httpx-aiohttp>=0.1.12
aiohttp>=3.13.0

# Audio processing
scipy>=1.10.0
numpy>=1.24.0

# CLI enhancements
rich>=13.0.0

# GUI - CustomTkinter for modern desktop interface
customtkinter>=5.2.0
pillow>=10.0.0

# Document conversion - Docling for PDF, DOCX, PPTX support
docling>=2.0.0
# Document generation
python-docx>=1.0.0
